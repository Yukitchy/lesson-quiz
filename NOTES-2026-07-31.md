# 2026-07-31 深夜バッチ調査ノート

## 結論: 残タスク2点はすでに解消済み（索引が古かっただけ）

`_memory/projects-active.md` の「残タスク: Sam板書特定・Rolando語数」は、7/30セッションのログ
（`ClaudeVault/_daily/2026-07-30/01-claudeセッションログ.md` 40行目）で既に「夜間の自動更新パイプラインで
解消済み」と記録されていた。今回そのログの記述を実測で再確認し、間違いないことを検証した。

### 「Sam板書特定」の正体と現状
生徒Samの板書データが見つかっていない／紐付いていない、という意味だったと推定。
実際には `data/sam.json` に32件の有効語彙（ja+en揃い）が存在し、`docs/sam-4158/index.html` にも
32語が焼き込み済み。`merge_vocab.py --pending` は0件（未処理レッスンなし）。
`processed_lessons.json` 台帳を見ると Sam の2ソースはどちらも処理済み:
- `2026-05-31-Sam-8月来日旅行計画.md` → `seeded`（台帳運用開始前からdata/sam.jsonに存在していた分。--seedで台帳にだけ登録）
- `2026-07-19-Sam-ジブリ美術館予約と空調服イギリス土産談義.md` → `merged:+16`

→ **特定済み・紐付き済み。追加作業不要。**

### 「Rolando語数」の正体と現状
生徒Rolandoの語数（単語数）カウントが未算出／おかしい、という意味だったと推定。
実際には `data/rolando.json` に23件の有効語彙が存在し、`docs/rolando-862c/index.html` にも
23語が焼き込み済み（MIN_ITEMS=4を余裕でクリアしビルド対象）。ソース3件すべて処理済み:
- `2026-06-29-Rolando-...` → `seeded`
- `2026-07-17-Rolando-...` → `merged:+10`
- `2026-07-20-Rolando-...` → `merged:+10`

→ **語数算出済み（23語）・ページ公開済み。追加作業不要。**

## 実施した検証（実測コマンドと結果）

```
$ python3 merge_vocab.py --pending
PENDING: 0
```

```
$ python3 -c "... json.load(sam.json/rolando.json) の valid件数カウント ..."
sam total 32 valid 32
rolando total 23 valid 23
```

```
$ grep -c '"ja":' docs/sam-4158/index.html docs/rolando-862c/index.html
sam-4158: 32
rolando-862c: 23
```

data側の語数とビルド済みHTMLへの焼き込み語数が一致 → パイプラインは正常に完走している。

さらに `docs/` を丸ごとコピーして `python3 build_quiz.py` を再実行 → 出力は `__BUILD__`（当日日付スタンプ）
以外バイト差分なし。既存出力を壊していないことを確認済み（検証用コピーは削除、日付だけの差分は
`git checkout -- docs/` で元に戻し、無意味なdiffを残さないようにした）。

`git status` はクリーン（このバッチでは実データ変更なし）。

## 直した内容
なし（コードもデータも変更不要と判明したため、ファイル変更は行っていない）。

## 置いた仮定
- 「Sam板書特定」＝Samの板書データの発見・生徒への紐付けの意。
- 「Rolando語数」＝Rolandoの語彙語数カウントの算出・確認の意。
- どちらも `_memory/projects-active.md` の残タスク欄が7/30の実態変化（自動マージ＋build_quiz実行）に
  追随できていなかっただけで、コード・データ側の不具合ではないと判断。

## TODO（コード側の作業ではなく記録の後始末）
- `_memory/projects-active.md` の「生徒別・板書連動クイズ」項目の残タスク行を「なし（7/31時点で完了確認済み）」
  等に更新するのが望ましいが、このPJの作業ディレクトリ外（Vault）の編集になるため本バッチでは実施していない。
  次回このPJを開いたセッションで一言直すだけで済む。
